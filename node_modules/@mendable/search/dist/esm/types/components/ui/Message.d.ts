/// <reference types="react" />
import { MendableSearchStyle } from '../types';
interface Props {
    message: string;
    message_id: number | null;
    sources: Array<any>;
    isBot: boolean;
    messageStyle: MendableSearchStyle;
    sourcesStyle: MendableSearchStyle;
}
export declare const Message: ({ message, message_id, messageStyle, sourcesStyle, isBot, sources }: Props) => JSX.Element;
export {};
